using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;

namespace FlatRedNetwork
{
    /// <summary>
    /// The roles supported for peers on the network
    /// </summary>
    public enum NetworkRole
    {
        Server,
        Client
    }
}
